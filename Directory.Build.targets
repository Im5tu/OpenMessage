<Project>
  <Target Name="CheckForOutDatedProjects" AfterTargets="Build" Condition="$(ProjectDir) != ''">
    <Exec Command="dotnet list package --outdated" WorkingDirectory="$(ProjectDir)" />
  </Target>
</Project>